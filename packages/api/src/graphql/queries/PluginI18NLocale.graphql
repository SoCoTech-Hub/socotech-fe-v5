
query GetPluginI18NLocale($limit: Int!) {
  plugini18nlocale(pagination: { limit: $limit }) {
    data {
      id
      attributes {
        code
        createdAt
        createdBy
        locale
        localizations
        name
        publishedAt
        updatedAt
        updatedBy
      }
    }
  }
}
