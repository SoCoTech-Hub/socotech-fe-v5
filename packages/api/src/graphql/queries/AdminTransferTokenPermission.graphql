
query GetAdminTransferTokenPermission($limit: Int!) {
  admintransfertokenpermission(pagination: { limit: $limit }) {
    data {
      id
      attributes {
        action
        createdAt
        createdBy
        locale
        localizations
        publishedAt
        token
        updatedAt
        updatedBy
      }
    }
  }
}
