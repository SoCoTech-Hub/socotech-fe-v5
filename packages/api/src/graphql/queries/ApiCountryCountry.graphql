
query GetApiCountryCountry($limit: Int!) {
  apicountrycountry(pagination: { limit: $limit }) {
    data {
      id
      attributes {
        createdAt
        createdBy
        currency
        locale
        localizations
        name
        publishedAt
        shortCode
        updatedAt
        updatedBy
      }
    }
  }
}
